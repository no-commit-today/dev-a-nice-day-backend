spring:
  application:
    name: tech-swipe-api

  profiles:
    active: local

  config:
    import:
      - monitoring.yml
      - storage-mysql.yml

---
spring:
  config:
    activate:
      on-profile: local

logging:
  level:
    org.apache.coyote.http11: debug
---
spring:
  config:
    activate:
      on-profile: local-dev

---
spring:
  config:
    activate:
      on-profile: dev

---
spring:
  config:
    activate:
      on-profile: prod
